name: bloom_client
description: Project Bloom client application.
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  isar: ^3.1.0
  isar_flutter_libs: ^3.1.0
  path_provider: ^2.1.2
  supabase_flutter: ^2.6.0
  flutter_dotenv: ^5.1.0
  google_generative_ai: ^0.4.0
  flutter_local_notifications: ^18.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: any
  isar_generator: ^3.1.0

flutter:
  uses-material-design: true
  assets:
    - .env
